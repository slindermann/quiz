services:
  z-quiz:
    build: .
    restart: unless-stopped
    ports:
      - "3000:3000"
    volumes:
      - ./data/quiz.db:/app/quiz.db
      - ./data/uploads:/app/uploads
    environment:
      - NODE_ENV=production
    env_file:
      - .env
